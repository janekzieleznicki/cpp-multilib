{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake build all",
            "command": "/usr/bin/cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build",
                "--config","Debug",
                "--target","all",
                "--", "-j", "12"
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}